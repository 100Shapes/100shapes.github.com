// Generated by CoffeeScript 1.4.0
var $about, $about_btn, $contact, $intro_btn, $nav, $nav_about, $nav_contact, $nav_links, $nav_logo, $nav_logo_pink, $nav_work, $window, $work, currentAnchor, currentSection, light, nav_flip, pink, scroll_duration,
  _this = this;

$window = $(window);

$nav = $('nav');

$nav_links = $('nav a');

$nav_about = $('.nav-about');

$nav_work = $('.nav-work');

$nav_contact = $('.nav-contact');

$nav_logo = $('.nav-logo');

$nav_logo_pink = $('.nav-logo.pink');

$about = $('.about');

$work = $('.work');

$contact = $('.contact');

$intro_btn = $('.intro a');

$about_btn = $('.about a');

scroll_duration = 800;

nav_flip = 640;

pink = '#C47382';

light = '#eee';

$(document).ready(function() {
  $('.carousel').carousel({
    interval: 2000
  });
  return $('#about').on('activate', function(evnt) {
console.log('hello');});
});

currentAnchor = $(evnt.currentTarget).find('a').attr('href');

currentSection = currentAnchor.split('#')[1];

if (currentSection === 'products') {

} else {

}

false;

$intro_btn.click(function(e) {
  e.preventDefault();
  return $about.ScrollTo({
    duration: scroll_duration
  });
});

$about_btn.click(function(e) {
  e.preventDefault();
  return $work.ScrollTo({
    duration: scroll_duration
  });
});

$nav_logo.click(function(e) {
  e.preventDefault();
  return $('body').ScrollTo({
    duration: scroll_duration
  });
});

$nav_about.click(function(e) {
  e.preventDefault();
  return $about.ScrollTo({
    duration: scroll_duration
  });
});

$nav_work.click(function(e) {
  e.preventDefault();
  return $work.ScrollTo({
    duration: scroll_duration
  });
});

$nav_contact.click(function(e) {
  e.preventDefault();
  return $contact.ScrollTo({
    duration: scroll_duration
  });
});

$window.scroll(function() {
  if (pageYOffset > nav_flip) {
    $nav.css({
      'background-color': light,
      'border-bottom-style': 'solid'
    });
    $nav_links.css('color', pink);
    return $nav_logo.addClass('pink');
  } else {
    $nav.css({
      'background-color': 'transparent',
      'border-bottom-style': 'none'
    });
    $nav_links.css('color', "#fff");
    return $nav_logo.removeClass('pink');
  }
});
